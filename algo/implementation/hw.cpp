#include <stdio.h>
using namespace std;

int main(){
    int i, j, k = 0;
    char a[11];
    char b[5];
    char c[11];
    gets(a):
    gets(b):

    for (i = 0; i <= 1; i++)
    {
        for (j = 0; j <= 9; j++)
        {
            if (a[j] == b[i])
            {
                temp = a[j] -32;
                a[j] = temp;
            }
        }
    }
    puts(a)
}
